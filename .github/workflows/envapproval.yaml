name: env approval
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout code
      - name: Deploying Code to env
        run: echo "Deploying"
