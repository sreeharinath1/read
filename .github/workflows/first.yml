name: My first workflow

on:
  push:
    branches:
      - main
jobs:
  build:
     runs-on: ubuntu-latest
     steps:
       - name: Just Print Hello
         run: echo "Hello, Iam learning Github Actions"
